<%# REQ: total_count %>

<% if (total_count >= 20) { %>
  <div class="module__pager"></div>
  <script>
    const pager = document.querySelector('.module__pager');
    const totalCount = <%= total_count %>;
    const pageStep = 20;
  </script>
  <script src="/javascripts/pager.js"></script>
<% } %>
