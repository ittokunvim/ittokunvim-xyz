<h1><%= title %></h1>

<div class="post__list">
  <% posts.forEach(post => { %>
    <div id=<%= 'post_' + post.id %>>
      <a href=<%= '/posts/' + post.id %>>
        <h2><%= post.title %></h2>
      </a>
      <p>作成時：<%= post.created_at %></p>
    </div>
  <% }) %>
</div>

