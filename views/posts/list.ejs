<h1><%= title %></h1>

<div class="post__list">
  <% posts.forEach(post => { %>
    <div id=<%= 'post_' + post.id %>>
      <h2>
        <a href=<%= '/posts/' + post.id %>><%= post.title %></a>
      </h2>
      <p>作成時：<%= post.created_at %></p>
    </div>
  <% }) %>
</div>

